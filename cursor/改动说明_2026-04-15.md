# TradeSnake 改动说明（2026-04-15）

本文档由 Cursor 助手整理，记录 **2026 年 4 月 15 日** 相关操作与仓库内尚未提交的改动概览，便于回顾与交接。

---

## 一、本会话中助手直接完成的事项

### 1. Tushare 本地落盘位置梳理

在代码与 `data/` 目录中核对后，结论摘要如下（根路径均为项目下的 `data/`，部分代码中为绝对路径 `/home/ailearn/projects/TradeSnake/data`）：

| 类型 | 路径 / 模式 | 内容说明 |
|------|-------------|----------|
| DuckDB | `data/historical.duckdb` | 日 K、交易日历等（含 Tushare 同步写入） |
| SQLite | `data/tradesnake.db` | `financial_history`、`ex_right_factor` 等与 Tushare 填充相关的表 |
| JSON 缓存 | `data/tushare_*.json` | `DataManager.get_tushare_data` 等写入的 Tushare 类型缓存 |
| JSON 缓存 | `data/history_price_*_*.json` | 经 Tushare 拉取历史价后的缓存 |
| 其他 | `data/*_cache.json`、`financial_*.json` 等 | 与多数据源混用有关，需结合 `source` 字段判断 |

### 2. Tushare 数据本地备份

- 已创建备份目录：`backups/tushare_local_20260415_175739/`
- 该目录内拷贝了：
  - `historical.duckdb`
  - `tradesnake.db`
  - 当时存在的全部 `tushare_*.json`、`history_price_*.json`（本机仅各 1 个示例文件）
- 说明：未默认拷贝 `financial_*.json` / `fin_*_cache.json`，因来源常为东财、Baostock 等与 Tushare 混合；若需整目录镜像可另行 `rsync data/`。

> 注：若你在备份后又生成了 `backups/tushare_local_20260415_175739.7z` 等压缩包，属于本地额外操作，不在助手自动步骤内。

### 3. 本说明文档

- 目录：`cursor/`
- 文件：`cursor/改动说明_2026-04-15.md`（即本文件）

---

## 二、工作区中与近期开发相关的代码与文档（未提交）

以下来自 `git status` / `git diff --stat`，**可能跨多轮会话累积**，与「仅今天」不必一一对应，但便于统一归档。

### 新增（未跟踪）

| 路径 | 说明 |
|------|------|
| `backend/stock_selector/market_snapshot.py` | 市场快照相关模块（新建） |
| `backups/` | 含上述 Tushare 备份子目录 |
| `scripts/backfill_core_pool.py` | 脚本 |
| `scripts/fix_cp_history.py` | 脚本 |
| `scripts/fix_today_cp_history.py` | 脚本 |
| `scripts/generate_predictions.py` | 脚本 |

### 已修改的主要源码与文档（节选）

| 路径 | 概要 |
|------|------|
| `backend/api/main.py` | 启动顺序、预加载、后台池任务与调度等 |
| `backend/data_manager/duckdb_store.py` | DuckDB 侧能力扩展（如批量成交额等） |
| `backend/data_manager/fetcher.py` | 数据源说明/注释等 |
| `backend/data_manager/update_scheduler.py` | 与选股回调、调度对齐 |
| `backend/stock_selector/pool_manager.py` | 池层级与 `to_temp` 等逻辑 |
| `backend/stock_selector/stock_selector.py` | 池刷新、初始化与财务相关行为 |
| `README.md`、`docs/README.md` | 项目说明与文档索引 |
| `docs/plans/PROJECT_OVERVIEW.md` | 总览（含产品范围等） |
| `docs/plans/data_manager/DATA_MANAGER_ARCHITECTURE.md` | 数据层架构 |
| `docs/plans/engine/ENGINE_ARCHITECTURE.md` | 引擎架构 |
| `docs/plans/stock_selector/STOCK_SELECTOR_ARCHITECTURE.md` | 选股器架构 |

### 数据缓存类改动

- `data/fin_*_cache.json`（大量）、`data/stock_list_cache.json` 等为运行产生的缓存更新，**通常不建议纳入版本库**；提交前可按团队规范决定是否还原或加入 `.gitignore`。

---

## 三、后续可选项（未在本次执行）

- 将 `cursor/` 加入版本库或写入 `.gitignore`，按团队习惯选择。
- 定期用同规则增量备份：`historical.duckdb`、`tradesnake.db`、`tushare_*.json`、`history_price_*.json`。
- 核对 `backend/tests` 与当前 API 路径是否一致（历史对话中曾提到个别路由/行为与测试不一致，未在本轮修改）。

---

*文档生成日期：2026-04-15*
