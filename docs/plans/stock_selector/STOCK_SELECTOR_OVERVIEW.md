# 股票筛选模块架构方案 v19.9

> 本文档描述 `stock_selector/` 模块的设计方案

---

## 版本历史 (v19.9 更新)

> **v19.9.9**: 状态持久化 - 添加 `save_state()`/`load_state()` 方法，池状态保存到 SQLite
> **v19.9.8**: 分钟K线自动填充 - 收盘后轮换填充核心池+活跃池股票（每天50只）
> **v19.9**: DuckDB 稳定性 - 文件锁(fcntl.flock) + 单连接复用
> **v19.5.6**: **实现核查**：stock_selector 模块实现与方案一致，无重大问题

## 输入输出

### 输入
| 来源 | 数据内容 |
|------|----------|
| data_manager | 股票列表、行情数据、财务数据、指数成分 |
| 手动配置 | 白名单/黑名单用户自定义配置 |

**产品范围**：仅沪深主板（不含创业板、科创板、北交所）

### 输出
| 输出内容 | 使用者 |
|----------|--------|
| 股票池分层 (core/active/observe/temp) | engine/recommender/data_manager |
| 池状态变化通知 | engine/recommender（刷新缓存） |

### 实现同步说明（v19.9，2026-04-23）

| 项 | 实现要点 |
|----|----------|
| `market_snapshot.py` | 将行情数据转为 `market_data[code]`（日均成交额万元、成交量排名、指数成分标记） |
| 启动时 `financial_data` | 启动时传入空 `{}`，财务数据在 `refresh_pools` 时补充 |
| 启动顺序 | `market_data` → 注册调度回调 → `StockSelector.initialize(...)` |
| `refresh_pools` 触发 | `pool_rebalance_background_task` 在收盘后执行，每日最多一次 |
| 池变更通知 | `on_pool_changed(tier, added, removed)` 按 tier 分别通知 |
| `get_stock_info(code)` | 对外查询当前池中 `StockInfo` |

---

## 一、模块定位

### 1.1 职责边界

```
                          ┌─────────────────────────────────────┐
                          │         双向数据流优化               │
data_manager ───────────▶ │ ◀────── 池状态通知 ──────▶          │
  (数据获取)              │       更新频率策略                  │
      ▲                  │                                   │
      │                  │  stock_selector ──────────▶ engine │
      │ 池状态决定         │    (池管理)              (战力分析) │
      │ 更新频率           │           │                      │
      └────────────────── │           ▼                      │
                         │     recommender                     │
                         │      (智能推荐)                     │
                         └─────────────────────────────────────┘
```

**核心职责**：
- 维护股票池分层结构（核心池、活跃池、观察池、临时池）
- 执行硬性排除和准入过滤
- 管理动态晋级/降级（含冲突处理、挤出机制）
- 处理事件触发（含去重、冷却）
- 向 data_manager 提供数据更新频率策略

**非职责**：
- 不负责战力计算（由 engine 提供）
- 不负责推荐逻辑（由 recommender 提供）
- **重要**：`stock_selector` 只读数据、不写数据，通过回调通知其他模块

### 1.2 模块价值

| 价值点 | 说明 |
|-------|------|
| **隔离关注点** | 筛选逻辑独立于数据获取和分析计算 |
| **复用性** | 被战力榜、推荐、回测等多个模块调用 |
| **可维护性** | 筛选规则调整不影响数据源和分析引擎 |
| **扩展性** | 便于新增筛选维度或调整分层策略 |

---

## 二、数据流设计

### 2.1 整体数据流

```
┌─────────────────────────────────────────────────────────────────┐
│                         数据输入                                  │
│  data_manager: 全量股票列表、行情数据、财务数据、指数成分         │
│  手动白名单/黑名单: 用户自定义配置                                │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                      第一层：硬性排除                             │
│  ST/退市/次新股(板块差异化)/僵尸股/停牌 + 黑名单                │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                      第二层：质量准入（底线）                     │
│  市值≥5亿 + 成交额≥1000万 + 财务健康(2/3)                     │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                      第三层：分层分类（递进）                     │
│   ┌──────────┐    ┌──────────┐    ┌──────────┐    ┌──────────┐  │
│   │  核心池   │    │  活跃池   │    │  观察池   │    │  临时池   │  │
│   │ 动态~300 │    │ 动态~500 │    │ 动态~1000│    │  事件驱动  │  │
│   │ ≥5000万  │    │ ≥2000万  │    │ ≥1000万  │    │  TTL:7日  │  │
│   └──────────┘    └──────────┘    └──────────┘    └──────────┘  │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                      输出：股票池                                │
│  engine: 核心池+活跃池 → 战力计算                               │
│  recommender: 全部池 → 推荐备选                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 2.2 盘后批处理流程 vs 盘中实时流程

```
盘后批处理（T日收盘后）：
  1. 数据更新 → 2. ST扫描 → 3. 指数同步 → 4. 池再平衡
  5. 财务预警 → 6. 临时池清理 → 7. 输出股票池

盘中实时处理（事件触发）：
  EventTrigger → to_temp() → 临时池（去重窗口期24小时）
```

### 2.3 数据更新策略联动（双向数据流）

核心：股票池状态反向指导 data_manager 的更新频率策略。

| 池 | 盘中更新频率 | 盘后更新频率 |
|----|-------------|-------------|
| **核心池** (~300只) | 5分钟 | 收盘后全量 |
| **活跃池** (~500只) | 30分钟 | 收盘后全量 |
| **观察池** (~1000只) | 不更新 | 收盘后日频 |

---

## 三、核心组件

### 3.1 组件概览

```
stock_selector/
├── config.py                # 筛选配置（含动态调整+更新频率）
├── stock_selector.py        # 主筛选器 (Facade)
├── pool_manager.py          # 股票池管理器
├── update_strategy.py       # 数据更新频率策略
├── filters/
│   ├── blacklist.py         # 黑名单过滤
│   ├── manual_list.py       # 手动白名单/黑名单
│   └── admission.py         # 准入条件（递进式）
├── rebalancer.py            # 动态再平衡（含挤出机制）
├── event_trigger.py         # 事件触发器（含去重）
├── index_sync.py            # 指数成分同步
├── financial_watcher.py     # 财务预警（含缓释/恢复）
├── history_keeper.py        # 历史数据保留
└── utils/
    ├── code_normalizer.py   # 股票代码标准化
    ├── rebalance_date.py    # 指数调整日计算
    └── pool_metrics.py      # 监控指标
```

### 3.2 核心类设计

#### PoolManager

- `get_pool(tier)` — 获取指定层股票代码列表
- `add_stock(code, tier, reason)` — 添加股票到指定池
- `remove_stock(code, reason)` — 从所有池中移除股票
- `get_stock_tier(code)` — 获取股票所在层
- `upgrade(code, reason)` — 晋级（含容量满时末位淘汰）
- `downgrade(code, reason)` — 降级
- `to_temp(code, event_type, reason)` — 临时池记录
- `process_temp(code, result)` — 处理临时池结果

#### Rebalancer

- `should_upgrade(code)` — 检查晋级条件（连续5日成交额、30日涨幅、指数成分、10日观察期）
- `should_downgrade(code)` — 检查降级条件（连续15日成交额低、ST触发、争议时降级优先）
- `execute_rebalance()` — 执行每日再平衡

#### EventTrigger

- `DEDUP_WINDOW_HOURS = 24` — 去重窗口期
- `check_event(code, market_data)` — 检查触发事件
- `should_trigger(code, event_type)` — 去重检查
- `handle_event(event)` — 处理触发事件

#### FinancialWatcher

- `WARNING_ACTIONS` — high立即降级、medium观察15日、low仅监控
- `RECOVERY_CONFIG` — 30日后检查恢复条件
- `check_warning(code, financial_data)` — 梯度判断预警
- `should_downgrade(code, warning)` — 梯度处理

### 3.3 代码标准化工具

```python
def normalize_code(code: str) -> str:
    """统一为6位纯数字：SH600519→600519, SZ000001→000001, BJ830799→830799"""
```

### 3.4 指数调整日计算

```python
def get_next_rebalance_dates(year: int) -> List[date]:
    """规则：6月和12月第二个星期五后的第一个交易日"""
```
